#include <iostream>
#include <string>

using namespace std;

int cT[256], len=0;

int main(){
	char c, k=0;
	while(cin.get(c)){

		cout << c << endl;

		// if(c!='"') cout << c;
		// else if(++k%2) cout << "``";
		// else cout << "''";
	}
}