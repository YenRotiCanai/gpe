#include<iostream>
using namespace std;

int divide(int n, int arr[], int &cnt){ //&cnt 是什麼意思，為什麼要加 &
	for(cnt = 0; cnt<)
}

int main(){
	int a, b;
	while(cin >> a >> b && (a!=0 || b!=0)){
		int lenA, lenB;
		int arrA[11] = {}, arrB[11] = {};
		int sum[12] = {};
		divide(a, arrA, lenA); //為什麼要多一個 lenA？
		divide(a, arrB, lenB);

	}
}